# MCP

## How to use

The MCP server can be used on different ways. Regardles the usage mode, run the scripts from the project root directory:


- To inspect server capabilities, run `mcp dev src/mcp/server.py`.
- To list all server capabilities from the client, run `uv run src/mcp_components/client.py`
